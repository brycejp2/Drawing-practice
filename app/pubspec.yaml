name: drawing_practice
description: "Drawing Practice: learn drawing, lettering, and pen control by tracing directed strokes."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  scoring:
    path: ../packages/scoring

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/content/
