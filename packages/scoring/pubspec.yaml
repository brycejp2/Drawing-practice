name: scoring
description: Pure Dart stroke-scoring engine for Drawing Practice. No Flutter dependency.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.9.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
